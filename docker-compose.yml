# Use root/example as user/password credentials
version: '3.1'

services:
  db:
    image: mysql:lts
    restart: always
    container_name: mysql_java
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
